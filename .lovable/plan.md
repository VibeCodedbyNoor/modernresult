

The browser tab shows "Lovable App" because the `index.html` title and meta tags were never updated to reflect your actual brand name.

**Changes needed in `index.html`:**
- Change `<title>` from "Lovable App" to "ResultPortal.online" (or your preferred brand name)
- Update `og:title` and `twitter:title` meta tags similarly
- Update `description` meta tags to something relevant like "Check your exam results online"
- Update `author` meta tag

This is a quick single-file edit.

